<div class="card">
    <div class="card-body">
        <h1>{{linguagens.titulo}}</h1>
        <br>
        <hr>
        <h6>{{linguagens.descricao}}</h6>
        <br>
        <small>Data de Publicação: {{#formatDate linguagens.data}}{{/formatDate}}</small>
    </div>
</div>